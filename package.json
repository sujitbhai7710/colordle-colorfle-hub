{
  "name": "colordle-colorfle-hub",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy:worker": "cd worker && npx wrangler deploy",
    "deploy:pages": "npx wrangler pages deploy dist --project-name=color-answers"
  },
  "dependencies": {
    "@astrojs/svelte": "^8.1.1",
    "astro": "^6.3.7",
    "svelte": "^5.33.14"
  }
}
